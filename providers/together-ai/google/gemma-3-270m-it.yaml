features:
    - system_messages
limits:
    context_window: 32768
    max_input_tokens: 32768
modalities:
    input:
        - text
    output:
        - text
mode: chat
model: google/gemma-3-270m-it
provisioning: provisioned
sources:
    - https://docs.together.ai/docs/fine-tuning-models
    - https://docs.together.ai/docs/dedicated-endpoints/models
status: active
supportedModes:
    - chat

# costs: not found in official docs — model is offered as a dedicated endpoint / fine-tuning base on Together AI, billed by GPU-hour rather than per-token
