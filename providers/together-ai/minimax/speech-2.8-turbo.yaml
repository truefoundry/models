costs:
    - input_cost_per_token: 0.0 # not found in official docs — dedicated inference deployment, no public per-token/character pricing
      output_cost_per_token: 0.0 # not found in official docs — dedicated inference deployment, no public per-token/character pricing
      region: "*"
modalities:
    input:
        - text
    output:
        - audio
mode: text_to_speech
model: minimax/speech-2.8-turbo
provisioning: provisioned
removeParams:
    - reasoning_effort
sources:
    - https://www.together.ai/models/minimax-speech-28
status: active
supportedModes:
    - text_to_speech
