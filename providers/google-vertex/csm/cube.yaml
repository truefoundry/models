modalities:
    input:
        - image
    output:
        - image
mode: image
model: csm/cube
provisioning: provisioned
sources:
    - https://console.cloud.google.com/vertex-ai/publishers/sesame/model-garden/csm
    - https://cloud.google.com/blog/products/ai-machine-learning/new-proprietary-models-vertex-model-garden
status: active
supportedModes:
    - image

# costs: not found in official docs — CSM Cube is a self-deploy partner model where pricing depends on the user's deployed compute (license + Google Cloud infrastructure), not per-token/per-image
# limits: not found in official docs
# features: not found in official docs
